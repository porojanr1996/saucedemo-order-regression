[pytest]
base_url = https://www.saucedemo.com
pythonpath = .
addopts = -ra --strict-markers
markers =
    smoke: login plus single-item purchase
    regression: order placement and checkout
    auth: login and access control
