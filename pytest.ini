[pytest]
base_url = https://www.saucedemo.com
pythonpath = .
addopts = -ra --strict-markers
markers =
    smoke: fast checks for the critical path (login + single purchase)
    regression: broader order-placement and checkout coverage
    auth: sign-in and access control
